# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.log

# Firebase cache
.firebase/

# Firebase config
# .firebaserc # We might want to track this if it points to a prod project, or ignore if per-dev
# For now, let's track it so dev knows which project is active

# Node modules
node_modules/

# OS junk
.DS_Store
Thumbs.db
